# Submission Checklist

- [ ] Deploy latest contracts (mode-enabled GoalVaultFactory).
- [ ] Update app and indexer env vars with deployed addresses.
- [ ] Verify create goal + deposit + withdraw on target network.
- [ ] Verify `/app/automation` can fetch status/decision and execute manual run.
- [ ] Capture and save one Hedera on-chain transaction hash + account ID proof.
- [ ] Run app in production mode for demo capture (`npm run build && npm run start`).
- [ ] Upload demo video (`fundory-demo.webm`) and collect public URL.
- [ ] Keep responsive proof screenshots (`responsive-check/goals-desktop.png`, `responsive-check/goals-mobile.png`).
- [ ] Ensure GitHub repo is public and link points to latest commit before deadline.
- [ ] Export pitch deck PDF (`submission/pitch-deck.pdf`).
- [ ] Submit Bounty form and Hedera track form.
